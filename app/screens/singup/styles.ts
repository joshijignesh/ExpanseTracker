import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
      flex: 1,
      justifyContent: 'center',
      padding: 20,
      backgroundColor: '#fff',
    },
    title: {
      fontSize: 28,
      fontWeight: 'bold',
      marginBottom: 40,
      textAlign: 'center',
    },
    txtFooter: {
      fontSize: 12,
      textAlign: 'right',
    },
    txtFooterLink: {
      fontSize: 12,
      fontWeight: 'bold',
    }
  });

  export {styles}