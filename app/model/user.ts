export interface User {
    id: number
    slug: string
    name: string
    phone: string
    email: string
    picture: string
    gender: string
    birth_date: string
    session_token: string
    is_active: boolean
}
